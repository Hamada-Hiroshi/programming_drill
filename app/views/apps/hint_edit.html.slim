.marked-wrapper
  span
    = link_to "基本情報編集ページに戻る", edit_app_path(@app), class: "btn btn-default"
  h2
    = @app.title
  h4
    | ヒント編集
  = form_with(model: @app, url: add_update_app_path) do |f|
    .marked-form
      .row
        .col-md-6
          h5
            | 記入欄（マークダウン記法で記述できます）
          .field
            #editor
              = f.text_area :hint, class: "textarea-form form-control", style: "min-height: 480px"
        .col-md-6
          h5
            | プレビュー
          #marked-area
    = f.submit "更新する", class: "btn btn-original"
