$('#average-rate').html("<%= j(render 'reviews/average_rate', app: @app) %>");
$('#review-form').html("<%= j(render 'review_form', my_review: @my_review, learning: @learning, app: @app) %>");
$('#error-messages').html("<%= j(render 'layouts/error_messages', model: @review) %>");
$('#review-index').html("<%= j(render 'review_index', reviews: @reviews) %>");

$('#star').raty({
  size: 36,
  starOff: "/star-off.png",
  starOn: "/star-on.png",
  starHalf: "/star-half.png",
  scoreName: 'review[rate]',
  half: true,
});

$('.star-rate').raty({
  size: 36,
  starOff: "/star-off.png",
  starOn: "/star-on.png",
  starHalf: "/star-half.png",
  half: true,
  readOnly: true,
  //scoreはデータ属性で渡している
});

$("#flash-message").html("<%= j(render 'layouts/flash_message') %>");
$('.header-flash').hide();
$('.header-flash').slideDown();