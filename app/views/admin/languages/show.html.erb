<div class="container admin-space">
  <div class="row">
    <div class="col-md-5">
      <h3 class="admin-title"><%= @language.name %></h3>
      <%= form_with(model: [:admin, @language], local: true) do |f| %>
        <%= render 'layouts/error_messages', model: @language %>

        <div class="field language-form">
          <%= f.label :name, "言語名編集" %>
          <%= f.text_field :name, class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "更新", class: "btn btn-original" %>
        </div>
        <%= link_to "この開発言語を削除する", admin_language_path(@language), method: :delete, class: "btn btn-original2", "data-confirm" => "本当に削除してよろしいですか？" %>
      <% end %>
    </div>

    <div class="col-md-5 col-md-offset-1">
    </div>
  </div>
</div>