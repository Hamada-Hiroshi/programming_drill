<% if model.errors.any? %>
  <div id="error-explanation">
    <h2>正しく入力されていない項目があります。</h2>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>